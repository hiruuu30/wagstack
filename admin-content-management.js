(()=>{
  if(location.pathname!='/admin')return;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const money=v=>'₱'+Number(v||0).toLocaleString('en-PH',{minimumFractionDigits:0,maximumFractionDigits:2});
  const cloud=()=>window.WagStackSupabase;
  let products=[],updates=[];

  const css=`
  .wa-cm-toolbar{display:flex;align-items:center;justify-content:space-between;gap:12px;margin:0 0 16px}.wa-cm-toolbar h2{margin:0}.wa-cm-btn{border:0;border-radius:12px;padding:10px 14px;background:#ff7a1a;color:#fff;font:800 11px Poppins;cursor:pointer}.wa-cm-btn.secondary{background:#eef1f4;color:#0b1e3f}.wa-cm-btn.danger{background:#fff0f0;color:#b42318}.wa-cm-list{display:grid;gap:10px}.wa-cm-row{display:grid;grid-template-columns:minmax(0,1.6fr) repeat(3,minmax(90px,.5fr)) auto;gap:12px;align-items:center;padding:14px 16px;border-radius:16px;background:rgba(255,255,255,.76);box-shadow:inset 0 0 0 1px rgba(11,30,63,.1)}.wa-cm-row--update{grid-template-columns:minmax(0,1.7fr) minmax(100px,.5fr) auto}.wa-cm-main strong{display:block;font-size:13px}.wa-cm-main small{display:block;margin-top:4px;font-size:10px;line-height:1.45;color:#738096}.wa-cm-meta{font-size:11px;color:#4f5d72}.wa-cm-actions{display:flex;gap:7px;justify-content:flex-end;flex-wrap:wrap}.wa-cm-mini{border:0;border-radius:10px;padding:8px 10px;background:#edf1f5;color:#0b1e3f;font:800 9px Poppins;cursor:pointer}.wa-cm-mini.danger{background:#fff0f0;color:#b42318}.wa-cm-status{display:inline-flex;align-items:center;gap:6px;font-size:10px;font-weight:800}.wa-cm-status:before{content:'';width:7px;height:7px;border-radius:50%;background:#22a06b}.wa-cm-status.off:before{background:#9aa4b2}.wa-cm-modal{position:fixed;inset:0;z-index:20000;display:grid;place-items:center;padding:20px;background:rgba(4,11,24,.58);backdrop-filter:blur(14px)}.wa-cm-dialog{width:min(620px,100%);max-height:90vh;overflow:auto;border-radius:24px;padding:22px;background:#fff;color:#0b1e3f;box-shadow:0 30px 90px rgba(4,11,24,.3)}.wa-cm-dialog h2{margin:0 0 16px}.wa-cm-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.wa-cm-field{display:grid;gap:6px;font-size:10px;font-weight:800}.wa-cm-field.full{grid-column:1/-1}.wa-cm-field input,.wa-cm-field textarea,.wa-cm-field select{width:100%;box-sizing:border-box;border:1px solid rgba(11,30,63,.14);border-radius:12px;padding:11px 12px;background:#fff;color:#0b1e3f;font:500 12px Poppins;outline:none}.wa-cm-field textarea{min-height:92px;resize:vertical}.wa-cm-check{display:flex;align-items:center;gap:8px;font-size:11px;font-weight:700}.wa-cm-dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}.wa-cm-empty{padding:28px;text-align:center;color:#7d8a9b;font-size:11px}.wa-cm-order{display:inline-flex;gap:5px;margin-left:6px}.wa-cm-order button{width:28px;height:28px;border:0;border-radius:8px;background:#eef1f4;color:#0b1e3f;cursor:pointer;font-weight:900}
  [data-theme='dark'] .wa-cm-row{background:linear-gradient(145deg,rgba(25,51,80,.9),rgba(13,35,60,.92));box-shadow:inset 0 0 0 1px rgba(255,255,255,.08)}[data-theme='dark'] .wa-cm-main small,[data-theme='dark'] .wa-cm-meta{color:#aebbd0}[data-theme='dark'] .wa-cm-mini,[data-theme='dark'] .wa-cm-btn.secondary,[data-theme='dark'] .wa-cm-order button{background:#183252;color:#e8eef8}[data-theme='dark'] .wa-cm-dialog{background:#102944;color:#f6f8fc}[data-theme='dark'] .wa-cm-field input,[data-theme='dark'] .wa-cm-field textarea,[data-theme='dark'] .wa-cm-field select{background:#17314f;color:#eef3fb;border-color:rgba(255,255,255,.12)}
  @media(max-width:900px){.wa-cm-row,.wa-cm-row--update{grid-template-columns:1fr}.wa-cm-actions{justify-content:flex-start}.wa-cm-grid{grid-template-columns:1fr}.wa-cm-field.full{grid-column:auto}}
  `;
  function styles(){if(document.getElementById('wa-cm-style'))return;const s=document.createElement('style');s.id='wa-cm-style';s.textContent=css;document.head.appendChild(s)}

  async function loadProducts(){products=await cloud().db.get('products','select=id,name,description,price,image_url,stock,active,category,created_at,updated_at&order=created_at.desc');renderProducts()}
  async function loadUpdates(){updates=await cloud().db.get('updates_carousel','select=id,eyebrow,title,body,image_url,cta_label,cta_href,sort_order,active,created_at,updated_at&order=sort_order.asc,created_at.asc');renderUpdates()}

  function renderProducts(){const host=document.querySelector('[data-cm-products]');if(!host)return;host.innerHTML=`<div class="wa-cm-toolbar"><div><h2>Shop products</h2><div class="wa-owner">Manage what customers can buy in WagStack Shop.</div></div><button class="wa-cm-btn" data-cm-new-product>+ Add product</button></div><div class="wa-cm-list">${products.length?products.map(p=>`<article class="wa-cm-row"><div class="wa-cm-main"><strong>${esc(p.name)}</strong><small>${esc(p.description||'No description')}</small></div><div class="wa-cm-meta">${esc(p.category||'Essentials')}</div><div class="wa-cm-meta">${money(p.price)} · ${Number(p.stock||0)} stock</div><div><span class="wa-cm-status ${p.active?'':'off'}">${p.active?'Live':'Hidden'}</span></div><div class="wa-cm-actions"><button class="wa-cm-mini" data-cm-edit-product="${esc(p.id)}">Edit</button><button class="wa-cm-mini ${p.active?'danger':''}" data-cm-toggle-product="${esc(p.id)}">${p.active?'Remove from shop':'Restore'}</button></div></article>`).join(''):'<div class="wa-cm-empty">No products yet.</div>'}</div>`}
  function renderUpdates(){const host=document.querySelector('[data-cm-updates]');if(!host)return;host.innerHTML=`<div class="wa-cm-toolbar"><div><h2>Updates carousel</h2><div class="wa-owner">Manage the cards shown in the customer home Updates carousel.</div></div><button class="wa-cm-btn" data-cm-new-update>+ Add update</button></div><div class="wa-cm-list">${updates.length?updates.map((u,i)=>`<article class="wa-cm-row wa-cm-row--update"><div class="wa-cm-main"><strong>${esc(u.title)}</strong><small>${esc(u.eyebrow||'UPDATE')} · ${esc(u.body||'')}</small></div><div><span class="wa-cm-status ${u.active?'':'off'}">${u.active?'Live':'Hidden'}</span><span class="wa-cm-order"><button title="Move up" data-cm-move-update="${esc(u.id)}" data-dir="-1" ${i===0?'disabled':''}>↑</button><button title="Move down" data-cm-move-update="${esc(u.id)}" data-dir="1" ${i===updates.length-1?'disabled':''}>↓</button></span></div><div class="wa-cm-actions"><button class="wa-cm-mini" data-cm-edit-update="${esc(u.id)}">Edit</button><button class="wa-cm-mini ${u.active?'danger':''}" data-cm-toggle-update="${esc(u.id)}">${u.active?'Remove':'Restore'}</button></div></article>`).join(''):'<div class="wa-cm-empty">No carousel updates yet.</div>'}</div>`}

  function modal(title,body,onSave){document.querySelector('.wa-cm-modal')?.remove();const wrap=document.createElement('div');wrap.className='wa-cm-modal';wrap.innerHTML=`<form class="wa-cm-dialog"><h2>${esc(title)}</h2>${body}<div class="wa-cm-dialog-actions"><button class="wa-cm-btn secondary" type="button" data-cm-cancel>Cancel</button><button class="wa-cm-btn" type="submit">Save</button></div></form>`;document.body.appendChild(wrap);wrap.querySelector('[data-cm-cancel]').onclick=()=>wrap.remove();wrap.onclick=e=>{if(e.target===wrap)wrap.remove()};wrap.querySelector('form').onsubmit=async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;try{await onSave(new FormData(e.currentTarget));wrap.remove()}catch(err){alert(err.message)}finally{b.disabled=false}}}
  function productModal(p=null){modal(p?'Edit product':'Add product',`<div class="wa-cm-grid"><label class="wa-cm-field full">Product name<input name="name" required value="${esc(p?.name||'')}"></label><label class="wa-cm-field full">Description<textarea name="description">${esc(p?.description||'')}</textarea></label><label class="wa-cm-field">Category<input name="category" value="${esc(p?.category||'Essentials')}"></label><label class="wa-cm-field">Price (PHP)<input name="price" type="number" min="0" step="0.01" required value="${esc(p?.price??'')}"></label><label class="wa-cm-field">Stock<input name="stock" type="number" min="0" step="1" required value="${esc(p?.stock??0)}"></label><label class="wa-cm-field">Image URL<input name="image_url" type="url" placeholder="https://…" value="${esc(p?.image_url||'')}"></label><label class="wa-cm-check full"><input name="active" type="checkbox" ${p?.active===false?'':'checked'}> Visible in shop</label></div>`,async fd=>{const payload={name:String(fd.get('name')||'').trim(),description:String(fd.get('description')||'').trim()||null,category:String(fd.get('category')||'Essentials').trim()||'Essentials',price:Number(fd.get('price')||0),stock:Number(fd.get('stock')||0),image_url:String(fd.get('image_url')||'').trim()||null,active:fd.get('active')==='on',updated_at:new Date().toISOString()};if(p)await cloud().db.update('products',`id=eq.${encodeURIComponent(p.id)}`,payload);else{delete payload.updated_at;await cloud().db.insert('products',payload)}await loadProducts()})}
  function updateModal(u=null){modal(u?'Edit update':'Add update',`<div class="wa-cm-grid"><label class="wa-cm-field">Eyebrow<input name="eyebrow" value="${esc(u?.eyebrow||'UPDATE')}"></label><label class="wa-cm-field">Order<input name="sort_order" type="number" step="1" value="${esc(u?.sort_order??((updates.at(-1)?.sort_order||0)+10))}"></label><label class="wa-cm-field full">Title<input name="title" required value="${esc(u?.title||'')}"></label><label class="wa-cm-field full">Body<textarea name="body">${esc(u?.body||'')}</textarea></label><label class="wa-cm-field">CTA label<input name="cta_label" value="${esc(u?.cta_label||'')}"></label><label class="wa-cm-field">CTA link<input name="cta_href" value="${esc(u?.cta_href||'')}"></label><label class="wa-cm-field full">Image URL / path<input name="image_url" value="${esc(u?.image_url||'')}"></label><label class="wa-cm-check full"><input name="active" type="checkbox" ${u?.active===false?'':'checked'}> Visible in Updates carousel</label></div>`,async fd=>{const payload={eyebrow:String(fd.get('eyebrow')||'').trim()||null,title:String(fd.get('title')||'').trim(),body:String(fd.get('body')||'').trim()||null,image_url:String(fd.get('image_url')||'').trim()||null,cta_label:String(fd.get('cta_label')||'').trim()||null,cta_href:String(fd.get('cta_href')||'').trim()||null,sort_order:Number(fd.get('sort_order')||0),active:fd.get('active')==='on',updated_at:new Date().toISOString()};if(u)await cloud().db.update('updates_carousel',`id=eq.${encodeURIComponent(u.id)}`,payload);else{delete payload.updated_at;await cloud().db.insert('updates_carousel',payload)}await loadUpdates()})}

  function mount(){styles();const nav=document.querySelector('.wa-nav'),main=document.querySelector('.wa-main');if(!nav||!main)return;if(!nav.querySelector('[data-wa="products"]'))nav.insertAdjacentHTML('beforeend','<button data-wa="products">Shop Products</button><button data-wa="updates">Updates Carousel</button>');if(!main.querySelector('[data-view="products"]'))main.insertAdjacentHTML('beforeend','<section class="wa-view" data-view="products"><div data-cm-products><div class="wa-loading">Loading products…</div></div></section><section class="wa-view" data-view="updates"><div data-cm-updates><div class="wa-loading">Loading updates…</div></div></section>')}

  document.addEventListener('click',async e=>{
    if(location.pathname!='/admin')return;
    const tab=e.target.closest('[data-wa="products"],[data-wa="updates"]');if(tab){mount();if(tab.dataset.wa==='products')loadProducts();else loadUpdates()}
    if(e.target.closest('[data-cm-new-product]'))productModal();
    const ep=e.target.closest('[data-cm-edit-product]');if(ep){const p=products.find(x=>x.id===ep.dataset.cmEditProduct);if(p)productModal(p)}
    const tp=e.target.closest('[data-cm-toggle-product]');if(tp){const p=products.find(x=>x.id===tp.dataset.cmToggleProduct);if(p){await cloud().db.update('products',`id=eq.${encodeURIComponent(p.id)}`,{active:!p.active,updated_at:new Date().toISOString()});await loadProducts()}}
    if(e.target.closest('[data-cm-new-update]'))updateModal();
    const eu=e.target.closest('[data-cm-edit-update]');if(eu){const u=updates.find(x=>x.id===eu.dataset.cmEditUpdate);if(u)updateModal(u)}
    const tu=e.target.closest('[data-cm-toggle-update]');if(tu){const u=updates.find(x=>x.id===tu.dataset.cmToggleUpdate);if(u){await cloud().db.update('updates_carousel',`id=eq.${encodeURIComponent(u.id)}`,{active:!u.active,updated_at:new Date().toISOString()});await loadUpdates()}}
    const mv=e.target.closest('[data-cm-move-update]');if(mv){const idx=updates.findIndex(x=>x.id===mv.dataset.cmMoveUpdate),dir=Number(mv.dataset.dir),other=idx+dir;if(idx>=0&&other>=0&&other<updates.length){const a=updates[idx],b=updates[other],av=Number(a.sort_order||0),bv=Number(b.sort_order||0);await Promise.all([cloud().db.update('updates_carousel',`id=eq.${encodeURIComponent(a.id)}`,{sort_order:bv,updated_at:new Date().toISOString()}),cloud().db.update('updates_carousel',`id=eq.${encodeURIComponent(b.id)}`,{sort_order:av,updated_at:new Date().toISOString()})]);await loadUpdates()}}
  });

  const mo=new MutationObserver(()=>mount());mo.observe(document.documentElement,{childList:true,subtree:true});mount();
})();